Setting up a local host server for development: 
(Required for local development as the browser requires a server in order to delay script execution with promises until all the images are loaded.)
* Install python.
* Install python extension in VSCode.
* Execute "python -m http.server" in the terminal.
* Navigate to http://localhost:8000/Visual%20Tracker/HTML/map_seasons.html in your browser.


Colours:
* Seasons red: #B92428 RGB(185, 36, 40)
* Ages blue: #4E4B98 RGB(78, 75, 152)


Northern Peak foothills:
* The mocked-up seasons actually have fake tree sprites. If you look in the upper corners, the background terrain should be blank,
	but I adapted the terrain from the "dead winter" trees that appear here by default (and in Temple Remains in Winter).